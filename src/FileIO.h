#include <iostream>
#include <fstream>
#include <sstream>
#include <string>
#include <iterator>
#include <vector>

using namespace std;

std::string LoadFile( char* Filepath );
std::vector<float> LoadObj( char* Filepath );